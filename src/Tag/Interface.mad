import type { Tag } from "@/Tag"



export type Annotated a = Annotated(Tag, a)

interface Tagged a {
  exhibit :: Tag -> a -> Annotated a
}

// inhibit :: Show a => Annotated a -> a
// pullTag :: Show a => Annotated a -> Tag
