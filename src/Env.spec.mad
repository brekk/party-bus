import { report } from "Study"
import { Tag } from "freitag/Tag"
import { ExactQuery } from "freitag/Tagged"

import { getEnvWithKey } from "@/Env"



TEST_DICT = {{
  "aardvark": "always",
  "boar": "best:nicest:coolest",
  "DEBUG": "best:nicest:coolest",
}}
report(
  getEnvWithKey($, TEST_DICT),
  "getEnvWithKey",
  [
    #["aardvark", [ExactQuery(Tag(["always"]))]],
    #["boar", [ExactQuery(Tag(["best", "nicest", "coolest"]))]],
    #["chimpanzee", []],
  ],
)
