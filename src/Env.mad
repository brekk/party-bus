import type { Tagged } from "freitag"

import Dict from "Dictionary"
import { fromMaybe } from "Maybe"



getEnvWithKey :: String -> Dictionary String String -> List (Tagged a)
export getEnvWithKey = (key, env) => pipe(
  Dict.get(key),
  map(manyFromString),
  fromMaybe([]),
)(env)
